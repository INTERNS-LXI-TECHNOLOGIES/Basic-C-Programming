#include<stdio.h>
int main()
{
int a[10],n,i,count=0;
printf("enter 10 elements :");
for(i=0;i<10;i++)
scanf("%d",&a[i]);
 printf(" enter the number which you want to count:");
scanf("%d",&n);
for(i=0;i<10;i++)
{
if(a[i]==n)
count++;
}
printf("\n count = %d",count);
return 0;
}
