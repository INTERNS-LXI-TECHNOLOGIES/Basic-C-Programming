#include<stdio.h>
int main()
{
int j,i,n,temp, array[250];
printf("enter the number of  elements to be sorted:");
scanf("%d",&n);
for(i=0,i<n;++i)
{
printf("%d. enter element :"i+1);
scanf("%d",&array[i]);
}
for(j=0;j<n-1;++j)
for(i=0;i<n-j-1;++i)
{
if(array[i]>array[i+1])
{
temp=array[i];
array[i]=array[i+1];
array[i+1]=temp;
}
}
printf(" the sorted array :\n");
for(i=0;i<n;i++)
printf("%d",array[i]);
return 0;
}
