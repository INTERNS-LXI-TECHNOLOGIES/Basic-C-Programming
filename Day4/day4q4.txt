#include <stdio.h>
int string(char[]);
int main() {
    char s[100];
    int c;
    printf("Enter the string\n");
    gets(s);
    c=string(s);
       printf("Length of the string: %d", c);
    
    }
    int string(char a[100]){
int i;
    
    for(i=0; a[i]!='\0';i++){
    
 
    	}
    return i;
}