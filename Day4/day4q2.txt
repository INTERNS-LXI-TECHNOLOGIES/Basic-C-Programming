#include <stdio.h>
void multiplication();
int main()
{
multiplication();
	return 0;
	}
	void multiplication(){
		int num1,i,n,multiplication;
		printf("Enter the number\n");
		scanf("%d",&num1);
		printf("Enter limit\n");
		scanf("%d",&n);
		for(i=1;i<=n;i++)
		{
		multiplication=num1*i;
			printf("%d*%d=%d\n",num1,i,multiplication);	
		}
		}
		