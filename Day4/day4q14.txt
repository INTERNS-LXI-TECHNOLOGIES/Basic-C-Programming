#include <stdio.h>
int array(int);
int main(){
	int limit1,result;
	printf("Enter your limit\n");
	scanf("%d",&limit1);
	//array(limit1);
	result=array(limit1);
	printf("The sum of all element is : %d ",result);
	return 0;
	}
	int array(int limit2){
		int i,a[100],sum=0;
		
	printf("\nEnter %d numbers\n",limit2);
		for(i=1;i<=limit2;i++){
			scanf("%d\n",&a[i]);
			}
			for(i=1;i<=limit2;i++){
			sum=sum+a[i];
			
		}
		
		return sum;
		}
