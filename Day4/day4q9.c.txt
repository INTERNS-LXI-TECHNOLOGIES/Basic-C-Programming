#include <stdio.h>
int str(int);
int main(){
	int num1;
	printf("Enter the number\n");
	scanf("%d",&num1);
	str(num1);
	if(num1==str(num1)){
	printf("\n strong number");
		}
	else
	printf("\n not a strong number");
	return 0;
	}
	int str (int num2){
		int i,r,temp,fact,sum=0;
		temp=num2;
		while(num2>0)
		{
		r=num2%10;
			fact=1;
			for(i=r;i>=1;i--)
			{
				fact=fact*i;
			}	
			sum=sum+fact;
			num2=num2/10;
			//num2=temp;
			
		}
		return sum;
		}
