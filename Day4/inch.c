#include<stdio.h>
float inch(float)
int main()
{
    float inches,feet
    printf(" the length in feet is %f\n",feet);
    feet = inch;
}
float inch(float)
{
    float feet,inches;
    printf("enter  the length in inches");
    scanf("%f",&inches);
    feet = (0.083) * inch;
    
    return feet;
}
