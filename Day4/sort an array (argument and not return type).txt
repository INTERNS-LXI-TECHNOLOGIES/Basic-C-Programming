#include <stdio.h>
 void bubble_sort( array[],  n)
{
  int i, j, temp;
 
  for (i=0;i<(n-1);i++)
  {
    for(j=0;j<n-i-1;j++)
    {
      if (array[j] > array[j+1])
      {
        temp = array[d];
        array[j]=array[j+1];
        array[j+1]=temp;
      }
    }
  }
}
 
int main()
{
   array[100], n, i, j, swap;
 
  printf("Enter number of elements\n");
  scanf("%ld", &n);
  printf("Enter %ld longegers\n", n);
  for (i= 0;i<n;i++)
    scanf("%ld", &array[i]);
  bubble_sort(array, n);
  printf("Sorted list in ascending order:\n") 
  for (i=0;i<n;i++)
     printf("%ld\n", array[i]);
  return 0;
}
 

 