#include<stdio.h>
int main()
{
int a,b,c;
printf(" enter the birth date of a, b, c:");
scanf("%d%d%d",&a,&b,&c);
if((a<=b)&&(a<=c))
{
printf("a's birth date is youngest in group");
}
if((b<=a)&&(b<=c))
{
printf("b's birth date is youngest in group");
}
if((c<=a)&&(c<=b))
{
printf("c's birth date is youngest in group");
}
return 0;
}
