#include <stdio.h>
#include <stdlib.h>

void main ()
{
int i,n,j,k,s1,s2,s3;
printf("Enter the min number = "  );
scanf("%d",&n);
 for(i=1;i<=n;i++)
 {
 	 s1=i*i;
 	  for(j=1;j<=n;j++)
		 {
 	 	s2=j*j;
 	  		for(k=1;k<=n;k++)
				 {
 	 				s3=k*k;
 	  				 if(s3==s1+s2)
 	  				    {
 	  				    	printf("%d  %d  %d\n",i,j,k);
						}
 				}
 		 }
 }

}