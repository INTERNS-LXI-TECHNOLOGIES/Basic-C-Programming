#include<stdio.h>
void main()
{
	int i,limit,n,x,r,f,sum=0;
	x=n;
	for(n=i, i<=limit, n++)
	while(n>0);
	{
		r=n%10;
		f=1;
		for (i=r, i>1, i--);
		{
			f=f*i;	
		}
		
		sum=sum+f;
		n=n/10;
	}
	n=x;
	if(n==sum)
	printf("\n The strong numbers are follows");
	
}