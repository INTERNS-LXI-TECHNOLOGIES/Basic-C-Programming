#include <stdio.h>

int main()
{
    int i,n,limit;
float fact,sum=0;

    printf("Enter the number: ");
    scanf("%d", &limit);
while(n<=limit)
{
fact=1;
    for(i=1; i<=n; i++)
    {
      fact=fact*i;
}

       sum=sum+( 1/ (float)fact);
float sum=0;
}
printf(" sume of the series is = %d \n", sum);

    return 0;
}